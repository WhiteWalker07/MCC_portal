# Optional — only needed if the Google Calendar integration is switched on
# (CALENDAR_ENABLED=1). Without it, services/calendar.py uses its logging stub
# and the portal runs normally. Uses per-member OAuth consent, not a service
# account — IT declined Workspace domain-wide delegation, see
# services/calendar.py.
#
#   pip install -r requirements-calendar.txt

-r requirements.txt
google-api-python-client==2.*
google-auth==2.*
